name: CI

on:
  push:
    branches:
      - master
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  APP_NAME: files_primary_s3
  PHP_VERSIONS: '["8.3"]'

jobs:
  get-vars:
    runs-on: ubuntu-latest
    outputs:
      app-name: ${{ env.APP_NAME }}
      php-versions: ${{ env.PHP_VERSIONS }}
    steps:
      - name: Set variables
        run: |
          echo "App name $APP_NAME"
          echo "PHP versions string: $PHP_VERSIONS"

  semantic-git-messages:
    name: Commits
    uses: owncloud/reusable-workflows/.github/workflows/semantic-git-message.yml@main

  php-code-style:
    name: PHP Code Style
    needs:
      - get-vars
    uses: owncloud/reusable-workflows/.github/workflows/php-codestyle.yml@main
    with:
      app-name: ${{ needs.get-vars.outputs.app-name }}
      php-versions: ${{ needs.get-vars.outputs.php-versions }}

  php-unit-scality:
    name: PHP Unit with Scality S3
    needs:
      - get-vars
    uses: ./.github/workflows/php-unit.yml
    with:
      app-name: ${{ needs.get-vars.outputs.app-name }}
      php-versions: ${{ needs.get-vars.outputs.php-versions }}
      use-scality-s3: true
      additional-packages: imagemagick

  php-unit-scality-multibucket:
    name: PHP Unit with Scality S3 Multibucket
    needs:
      - get-vars
    uses: ./.github/workflows/php-unit.yml
    with:
      app-name: ${{ needs.get-vars.outputs.app-name }}
      php-versions: ${{ needs.get-vars.outputs.php-versions }}
      use-scality-s3: true
      scality-s3-test-config: scality.multibucket
      additional-packages: imagemagick
